# 论文索引(29 篇,一句话中文概括)

> 对应 `docs/kg/90-papers/*.md`。每篇英文笔记含 frontmatter(`title/authors/year/url/loop-stage/tags`)+ TL;DR / 方法 / 主张+结果 / 与我们的相关性。下表按 loop 阶段分组;数字均带出处(见各英文笔记)。

## 基础 / 全景(foundations · landscape)

| 笔记 | 一句话 |
|---|---|
| `zha2023-data-centric-ai-survey` | 数据为中心 AI 的奠基综述:把提升数据质量与数量(而非模型结构)作为性能的首要杠杆。 |
| `yang2023-ai-generated-images-data-source` | 全面综述 AI 生成图像如何替代/增强真实数据用于视觉智能,涵盖生成模型、神经渲染与下游(检测、分割、自动驾驶)。 |
| `alimisis2024-diffusion-augmentation-review` | 扩散式图像增强专门综述:把方法整理成分类法,综述模型、评估指标与开放挑战。 |
| `nikouei2025-small-object-detection-survey` | 小目标检测全面综述:让小目标困难的挑战、应对的深度学习技术家族、领域的数据集/指标/应用。 |
| `azizi2023-synthetic-data-improves-imagenet` | 微调类条件扩散(Imagen)生成的合成 ImageNet 数据加进真实数据,可测量地提升分类准确率,胜过强 ResNet/ViT 基线。 |
| `ge2023-text2image-for-detection` | 文生图分别生成前景与背景再带掩码 cut-and-paste,产出足以媲美真实数据训练的检测/分割数据。 |
| `zhang2024-generated-data-amplify-bias` | 跨代把生成数据喂回训练(自我消费闭环)会放大子群偏见——对任何闭环增强系统的"有害"警示。 |

## 阶段 1 — 诊断(stage1)

| 笔记 | 一句话 |
|---|---|
| `eyuboglu2022-domino-slice-discovery` | 切片发现的奠基方法:用 CLIP 式跨模态嵌入找分类器系统性出错的连贯子集,并自动用自然语言描述每个切片。 |
| `chen2025-hibug2-error-slice-discovery` | 先标后切的调试框架:生成任务相关视觉属性、高效枚举错误切片,并能预测验证集之外的切片。 |
| `zhang2026-gh-esd-instance-slice-discovery` | 把错误切片发现推到实例级任务(检测、分割):找基于空间/关系视觉模式(而非仅属性组合)的失败切片。 |
| `chegini2023-clip-diffusion-failure-mitigation` | 端到端诊断→生成:用 LLM/VLM 无需人工标注地描述模型失败模式(如背景伪相关),再扩散生成针对性数据修复。 |

## 阶段 2 — Spec(stage2)

| 笔记 | 一句话 |
|---|---|
| `huang2024-actgen-active-generation` | 让生成"训练感知":用模型自己的错分类样本作为"该合成什么"的信号,生成类似困难案例的图像而非批量数据。 |
| `nguyen2025-tada-targeted-augmentation` | 只增强*难学*子集(训练早期没学会的样本)用忠实合成变体——胜过增强整个数据集(~30–40% 子集,+2.8%)。 |
| `chen2023-geodiffusion-geometric-control` | 把几何条件(边界框、相机视角)转成文本 prompt,让预训练扩散生成遵守指定 layout 的检测数据。 |
| `zhu2025-recon-region-controllable` | 让结构可控检测生成真正忠实:采样时纠正误生成区域、把每个区域对齐到其文本描述。 |

## 阶段 3 — 合成(stage3)

| 笔记 | 一句话 |
|---|---|
| `zhu2024-odgen-detection-generation` | 在领域数据上微调扩散并逐目标条件化(视觉前景块 + 逐目标文本),产出多类、遮挡检测场景,提升检测器(+25.3% mAP)。 |
| `tang2024-aerogen-remote-sensing-generation` | 从 layout 条件(水平 AND 旋转框)生成遥感检测数据,带多样性条件生成器加过滤——稀有类大增益(+12.6–17.8%)。 |
| `zhao2023-xpaste-copy-paste` | 从 SD(与网爬)取前景目标、CLIP 过滤再合成,扩展 copy-paste 增强——长尾类强增益(+6.8 box AP)。 |
| `ruiz2022-dreambooth-subject-driven` | 仅用 3–5 张某特定主体图像个性化文生图,绑到唯一 token,使主体能在新场景/姿态/光照下重新渲染。 |

## 阶段 4 — 闭环(stage4)

| 笔记 | 一句话 |
|---|---|
| `gerstgrasser2024-accumulate-avoid-collapse` | 模型坍缩源于跨代*替换*真实数据;若改为*累积*真实+所有合成代,可证明避免坍缩。 |
| `yi2025-escaping-collapse-verification` | 注入外部验证器(人或更强模型)进迭代重训可挡住坍缩——但若验证器不完美,增益会停滞甚至反转。 |
| `liu2024-synthvlm-clipscore-filtering` | 用高质 caption 扩散生成图像,再用 CLIPScore 过滤图文对——18% 精选合成胜过全量基线。 |
| `kessler2025-active-synthetic-data` | 在由学生模型当前状态引导的迭代闭环中生成合成微调数据,用简单主动学习选择,胜过静态一次性生成。 |

## 闭环模板 / 先验系统(landscape · 全闭环邻居)

| 笔记 | 一句话 |
|---|---|
| `ouyang2025-safefix-model-repair` | 诊断驱动闭环:把视觉模型系统性失败归因到被低估语义子群,生成针对性合成图像精确修复并重训。脊柱的已发表模板(分类)。 |
| `yurt2025-ltda-drive-longtail` | 三段式"替换增强"用于驾驶稀有类:扩散删头部类、生成模型原位插入尾部类、LLM agent 过滤(KITTI 稀有类 +34.75%)。最近的全闭环检测邻居。 |
| `petersen2025-scene-aware-location` | 学习概率模型预测新目标在场景里现实地该放*哪*,再 inpaint 上去——取代"复用帧 layout"或"随机放置"(+1.4 mAP)。与 G4 相关。 |
| `hong2024-galot-generative-active-learning` | 无真实数据闭合主动学习:用 AL 准则优化零样本 T2I 的 prompt 合成有信息量样本,文本派生伪标签训练视觉模型(分类、无框)。 |
| `girella2024-diag-indistribution-defect` | 免训练生成 in-distribution 合成缺陷:专家给文字 + 异常区域,潜空间扩散就地画上(KSDD2 +18–28% AP)。切片→spec 标记的手工版。 |
| `voetman2023-big-data-myth-detection` | 微调 SD 生成检测数据集、**人工标注**、真实图测试:合成训练的检测器与真实基线*相近*(AP 偏差 0.09–0.12)。怀疑派校准:替代品非放大器。 |
